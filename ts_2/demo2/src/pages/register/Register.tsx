import React from "react";
import {Typography} from "antd";

export const Register: React.FC = () => {
    return <div>
        <Typography.Title level={1}>Register Page</Typography.Title>
    </div>
}